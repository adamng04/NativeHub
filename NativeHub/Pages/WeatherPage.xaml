<Page
    x:Class="NativeHub.Pages.WeatherPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeHub.Pages"
    xmlns:models="using:NativeHub.Models"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded">
    <Grid Padding="24" RowSpacing="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Weather" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Current conditions, next 24 hours, and seven-day outlook" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <AutoSuggestBox
                x:Name="CityBox"
                MinHeight="44"
                PlaceholderText="Type a city"
                QueryIcon="Find"
                TextMemberPath="DisplayName"
                TextChanged="CityBox_TextChanged"
                SuggestionChosen="CityBox_SuggestionChosen"
                QuerySubmitted="CityBox_QuerySubmitted"
                AutomationProperties.Name="Weather city">
                <AutoSuggestBox.ItemTemplate>
                    <DataTemplate x:DataType="models:WeatherPlace">
                        <StackPanel Padding="4,2" Spacing="2">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind RegionLabel}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </DataTemplate>
                </AutoSuggestBox.ItemTemplate>
            </AutoSuggestBox>
            <Button Grid.Column="1" Content="Refresh" Click="Refresh_Click" />
        </Grid>
        <InfoBar x:Name="WeatherInfo" Grid.Row="2" Severity="Warning" IsClosable="True" AutomationProperties.LiveSetting="Assertive" />
        <ScrollViewer Grid.Row="3" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="16">
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="16" Padding="24">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="CityText" FontSize="24" FontWeight="SemiBold" />
                            <TextBlock x:Name="ConditionText" FontSize="16" />
                            <TextBlock x:Name="DetailsText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                            <TextBlock x:Name="SunText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <TextBlock x:Name="TemperatureText" Grid.Column="1" FontSize="54" FontWeight="SemiBold" AutomationProperties.LiveSetting="Polite" />
                    </Grid>
                </Border>

                <TextBlock Text="Next 24 hours" Style="{StaticResource SubtitleTextBlockStyle}" />
                <ItemsControl x:Name="HourlyItems" HorizontalAlignment="Stretch">
                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><ItemsWrapGrid Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerTransitions><TransitionCollection><RepositionThemeTransition /></TransitionCollection></ItemsControl.ItemContainerTransitions>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="local:WeatherHourView">
                            <Border Width="112" MinHeight="126" Margin="0,0,8,8" Padding="12" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="10">
                                <StackPanel Spacing="5"><TextBlock Text="{x:Bind Time}" FontWeight="SemiBold" /><TextBlock Text="{x:Bind Temperature}" FontSize="20" /><TextBlock Text="{x:Bind Condition}" TextWrapping="Wrap" /><TextBlock Text="{x:Bind Rain}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /></StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="Seven-day forecast" Style="{StaticResource SubtitleTextBlockStyle}" />
                <ItemsControl x:Name="ForecastItems" HorizontalAlignment="Stretch">
                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><ItemsWrapGrid Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerTransitions><TransitionCollection><RepositionThemeTransition /></TransitionCollection></ItemsControl.ItemContainerTransitions>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="local:WeatherDayView">
                            <Border Width="220" MinHeight="104" Margin="0,0,8,8" Padding="14" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="10">
                                <StackPanel Spacing="5">
                                    <TextBlock Text="{x:Bind Date}" FontWeight="SemiBold" />
                                    <TextBlock Text="{x:Bind Condition}" TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind Range}" FontSize="20" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        <TextBlock Grid.Row="4"><Run Text="Weather data by " /><Hyperlink NavigateUri="https://open-meteo.com/">Open-Meteo</Hyperlink><Run Text=" · Free non-commercial API · CC BY 4.0 data" /></TextBlock>
    </Grid>
</Page>
