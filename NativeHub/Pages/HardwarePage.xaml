<Page
    x:Class="NativeHub.Pages.HardwarePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeHub.Pages"
    xmlns:models="using:NativeHub.Models"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded">
    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Hardware" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock x:Name="UpdatedText" Text="Loaded once when opened · refresh manually" Foreground="{ThemeResource TextFillColorSecondaryBrush}" AutomationProperties.LiveSetting="Polite" />
            </StackPanel>
            <Button Grid.Column="1" Content="Copy summary" Click="Copy_Click" />
            <Button Grid.Column="2" Content="Export JSON" Click="Export_Click" />
            <Button x:Name="RefreshButton" Grid.Column="3" Content="Refresh" Click="Refresh_Click" ToolTipService.ToolTip="Refresh hardware information (F5)" />
        </Grid>
        <InfoBar x:Name="SensorInfo" Grid.Row="1" Title="Best-effort sensor access" Message="NativeHub stays unelevated. Restricted or unsupported readings remain labeled unavailable." Severity="Informational" IsOpen="True" IsClosable="True" />
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="16" Padding="16">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions><ColumnDefinition Width="72" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <FontIcon Glyph="&#xE7F8;" FontSize="48" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                <TextBlock x:Name="FastfetchText" Grid.Column="1" FontFamily="Cascadia Mono" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="3" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
            <ItemsRepeater x:Name="HardwareGroups" HorizontalAlignment="Stretch" AutomationProperties.Name="Hardware information groups">
                <ItemsRepeater.Layout>
                    <UniformGridLayout
                        Orientation="Horizontal"
                        MaximumRowsOrColumns="3"
                        MinItemWidth="340"
                        MinColumnSpacing="12"
                        MinRowSpacing="12"
                        ItemsStretch="Fill"
                        ItemsJustification="Start" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:HardwareGroupView">
                        <Border
                            MinHeight="160"
                            Padding="14"
                            HorizontalAlignment="Stretch"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="14">
                            <StackPanel Spacing="8">
                                <TextBlock Text="{x:Bind Title}" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                <ItemsControl ItemsSource="{x:Bind Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="models:HardwareItem">
                                            <Grid Margin="0,0,0,6" ColumnSpacing="10">
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="120" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="{x:Bind Value}" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
                                                    <TextBlock
                                                        Text="{x:Bind Detail}"
                                                        Visibility="{x:Bind HasDetail}"
                                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                        TextWrapping="Wrap" />
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
