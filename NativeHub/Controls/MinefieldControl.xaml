<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NativeHub.Controls.MinefieldControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="14" RowSpacing="10">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <ComboBox x:Name="DifficultyBox" Width="150" SelectionChanged="DifficultyBox_SelectionChanged" AutomationProperties.Name="Minefield difficulty">
                <ComboBoxItem Content="Beginner · 9 × 9" Tag="Beginner" IsSelected="True" />
                <ComboBoxItem Content="Intermediate · 16 × 16" Tag="Intermediate" />
                <ComboBoxItem Content="Expert · 30 × 16" Tag="Expert" />
            </ComboBox>
            <Button Grid.Column="1" Content="New game" Click="NewGame_Click" ToolTipService.ToolTip="New game (F2)" />
            <TextBlock x:Name="BestText" Grid.Column="3" VerticalAlignment="Center" Foreground="#FFB8C8DE" />
        </Grid>
        <Border Grid.Row="1" Padding="10,7" Background="#FF111B2B" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Text="Choose a cell to begin" FontWeight="SemiBold" AutomationProperties.LiveSetting="Polite" />
                <TextBlock x:Name="MineText" Grid.Column="1" Margin="12,0" FontFamily="Cascadia Mono" />
                <TextBlock x:Name="TimerText" Grid.Column="2" MinWidth="64" TextAlignment="Right" FontFamily="Cascadia Mono" />
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" ZoomMode="Disabled">
            <Grid x:Name="BoardGrid" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#FF0A101A" />
        </ScrollViewer>
        <Grid.KeyboardAccelerators>
            <KeyboardAccelerator Key="F2" Invoked="NewGameShortcut_Invoked" />
        </Grid.KeyboardAccelerators>
    </Grid>
</UserControl>
